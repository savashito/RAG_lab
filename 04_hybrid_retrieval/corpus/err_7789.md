Fault ERR_7789 indicates that the firmware image has failed its version compatibility check and the system has halted the boot process to prevent damage. Install a firmware build that matches the hardware revision.
